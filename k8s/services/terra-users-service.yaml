apiVersion: apps/v1
kind: Deployment
metadata:
  name: terra-users-service
  namespace: terrabia
spec:
  replicas: 2
  selector:
    matchLabels:
      app: terra-users-service
  template:
    metadata:
      labels:
        app: terra-users-service
    spec:
      containers:
      - name: terra-users-service
        image: nguembu/terrabia-terra-users-service:latest
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "terra_users_service.user_service.settings"
        - name: DEBUG
          value: "TRUE"
        - name: SPRING_CLOUD_CONFIG_URI
          valueFrom:
            configMapKeyRef:
              name: terrabia-global-config
              key: SPRING_CLOUD_CONFIG_URI
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: terrabia-global-config
              key: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
        - name: EUREKA_CLIENT_ENABLED
          value: "true"
        - name: EUREKA_CLIENT_FETCH_REGISTRY
          value: "true"
        - name: EUREKA_CLIENT_REGISTER_WITH_EUREKA
          value: "true"
        - name: DATABASE_URL
          value: "postgresql://delphan:mypassword@terra-users-db:5432/terra_users_db"
        - name: SERVICE_PORT
          value: "8084"
        ports:
        - containerPort: 8084
        livenessProbe:
          httpGet:
            path: /health/
            port: 8084
          initialDelaySeconds: 120
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health/
            port: 8084
          initialDelaySeconds: 90
          periodSeconds: 10
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: terra-users-service
  namespace: terrabia
spec:
  selector:
    app: terra-users-service
  ports:
  - port: 8084
    targetPort: 8084
  type: ClusterIP
