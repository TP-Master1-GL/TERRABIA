apiVersion: apps/v1
kind: Deployment
metadata:
  name: terra-auth-service
  namespace: terrabia
spec:
  replicas: 2
  selector:
    matchLabels:
      app: terra-auth-service
  template:
    metadata:
      labels:
        app: terra-auth-service
    spec:
      containers:
      - name: terra-auth-service
        image: nguembu/terrabia-terra-auth-service:latest
        env:
        - name: DJANGO_SETTINGS_MODULE
          value: "terra_auth_service.settings"
        - name: DEBUG
          value: "TRUE"
        - name: SPRING_CLOUD_CONFIG_URI
          valueFrom:
            configMapKeyRef:
              name: terrabia-global-config
              key: SPRING_CLOUD_CONFIG_URI
        - name: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
          valueFrom:
            configMapKeyRef:
              name: terrabia-global-config
              key: EUREKA_CLIENT_SERVICE_URL_DEFAULTZONE
        - name: EUREKA_CLIENT_ENABLED
          value: "true"
        - name: EUREKA_CLIENT_FETCH_REGISTRY
          value: "true"
        - name: EUREKA_CLIENT_REGISTER_WITH_EUREKA
          value: "true"
        - name: SECRET_KEY
          valueFrom:
            secretKeyRef:
              name: database-secrets
              key: django-secret-key
        - name: DATABASE_URL
          value: "mysql://terabia_user:terabia_password@terra-auth-db:3306/terabia"
        - name: SERVICE_PORT
          value: "8083"
        ports:
        - containerPort: 8083
        livenessProbe:
          httpGet:
            path: /health/
            port: 8083
          initialDelaySeconds: 120
          periodSeconds: 30
        readinessProbe:
          httpGet:
            path: /health/
            port: 8083
          initialDelaySeconds: 90
          periodSeconds: 10
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
---
apiVersion: v1
kind: Service
metadata:
  name: terra-auth-service
  namespace: terrabia
spec:
  selector:
    app: terra-auth-service
  ports:
  - port: 8083
    targetPort: 8083
  type: ClusterIP
